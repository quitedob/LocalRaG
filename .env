DEEPSEEK_API_KEY=sk-7c9c6d4abaf94eac8368a29527ffe7a3
DOTENV_LOADED=True
# D:\python_code\LocalAgent\.env

# --- Flask 配置 ---
# !! 生成一个你自己的强随机密钥替换掉下面的示例 !!
# !! Generate your own strong random key to replace the example below !!
SECRET_KEY='change_this_to_a_very_long_and_random_secret_string_12345!@#$%'

# --- 运行模式 ---
# 设置为 True 在开发时开启调试模式和自动重载
# Set to True during development for debugging and auto-reloading
FLASK_DEBUG=True
FLASK_CONFIG=development # 明确指定使用开发配置 (可选但推荐)

# --- Redis 配置 (如果你的 Redis 不是默认 localhost:6379) ---
REDIS_URL=redis://localhost:6379/0

# --- Celery 配置 (通常与 Redis 相同) ---
 CELERY_BROKER_URL=redis://localhost:6379/1 # 可以使用不同的 Redis DB
 CELERY_RESULT_BACKEND=redis://localhost:6379/2

